import styled from "styled-components"
import { CartDrawer, Form, Button } from "../../styles/modal"

export { Button }

export const PaymentContainer = styled(CartDrawer)``

export const FormPayment = styled(Form)`
    display: flex;
    flex-direction: column;
    gap: 16px;
`